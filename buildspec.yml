version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9
  pre_build:
    commands:
      - pip install awsebcli
      - pip install -r requirements.txt
  build:
    commands:
      - eb deploy
artifacts:
  files:
    - '**/*'
