version: 0.2

phases:
  pre_build:
    commands:
      # - pip install awsebcli boto3
      - pip install -r requirements.txt
  build:
    commands:
      - gunicorn -b 0.0.0.0:8000 main:app

artifacts:
  files:
    - '**/*'
